version: "3.9"

services:
  django:
    container_name: django
    build:
      context: ./django
      dockerfile: dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/usr/src
    ports:
      - "8000:8000"
