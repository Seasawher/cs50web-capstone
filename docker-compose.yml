version: "3.9"

services:
  django:
    build:
      context: ./django
      dockerfile: dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./django:/usr/src/django
    ports:
      - "8000:8000"
